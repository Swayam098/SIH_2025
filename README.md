# SIH_2025

A project by **N-Jishnu**

---

## 🗂 Project Structure

| Folder/File | Description |
|-------------|-------------|
| `home/` | Contains project work for the “home” module. |
| `home_new/` | Updated version or next iteration of the home module. |
| `smartbus-backend/` | Backend server / API code for “SmartBus” system. |
| `GRU.png`, `LSTM.png`, `MLP.png` | Model diagrams or visualizations (e.g. Gated Recurrent Unit, LSTM, Multi-Layer Perceptron). |

---

## 🚀 Getting Started

These instructions will help you set up the project locally.

### Prerequisites

- Git  
- Node.js / npm (if there's frontend / JS)  
- Python / required libraries (if ML / backend uses Python)  

### Setup

1. Clone the repository  
   ```bash
   git clone https://github.com/N-Jishnu/SH_2025.git
